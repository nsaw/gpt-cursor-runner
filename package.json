{
  "name": "gpt-cursor-runner",
  "version": "1.0.0",
  "description": "GPT-Cursor Runner with Slack integration and Cloudflare tunnel support",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "fly deploy",
    "logs": "fly logs",
    "status": "fly status",
    "inject-manifest": "node scripts/inject_slack_manifest_commands_v1.js",
    "update-manifest-cli": "node scripts/update_slack_manifest_cli.js",
    "migrate-granite": "node scripts/migrate-to-granite.js"
  },
  "keywords": [
    "gpt",
    "cursor",
    "slack",
    "webhook",
    "runner"
  ],
  "author": "Sawyer",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "crypto": "^1.0.1",
    "axios": "^1.6.0",
    "@slack/socket-mode": "^2.0.0",
    "@slack/web-api": "^6.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
