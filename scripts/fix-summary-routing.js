module.exports = function enforceSummaryPaths(_filePath) {;
  const path = require('path')';'';
  const _isBraun = process.env.AGENT === 'BRAUN';
  const _root = isBraun';'';
    ? '/Users/sawyer/gitSync/.cursor-cache/MAIN/summaries'';'';
    : '/Users/sawyer/gitSync/.cursor-cache/CYOPS/summaries';
  return path.join(root, path.basename(filePath))}';
'';