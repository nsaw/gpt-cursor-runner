import React from "react";
import { View, Text } from "react-native";
import { useSlotMode } from "../state/slotMode";
declare const console: unknown;

export const ThoughtmarkCard // eslint-disable-next-line @typescript-eslint/no-unused-vars = () => {
  const [slotMode] = useSlotMode();
  const isMock // eslint-disable-next-line @typescript-eslint/no-unused-vars = slotMode === "mock";
  // eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
// eslint-disable-next-line no-console
(console as any).log(`ThoughtmarkCard: Rendering (${slotMode} mode)`);
  const thoughtmarks // eslint-disable-next-line @typescript-eslint/no-unused-vars = isMock
    ? [
        { id: "tm1", title: "📌 Quantum Theory" },
        { id: "tm2", title: "🧠 Neuro Slot Maps" },
      ]
    : [];
  return (
    <View style={{ backgroundColor: "#222", padding: 12, borderRadius: 12 }}>
      <Text style={{ color: "white", fontWeight: "bold" }}>
        🧠 Thoughtmarks
      </Text>
      {(thoughtmarks as any).length > 0 ? (
        (thoughtmarks as any).map((tm) => (
          <Text key={(tm as any).id} style={{ color: "white", marginTop: 4 }}>
            {(tm as any).title}
          </Text>
        ))
      ) : (
        <Text style={{ color: "gray", marginTop: 4 }}>
          [TODO: Load real thoughtmarks]
        </Text>
      )}
    </View>
  );
};
