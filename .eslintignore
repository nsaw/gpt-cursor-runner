# Aggressive ESLint baseline (triage phase)
# Exclude all problematic areas to achieve Block-Until-Pass
dist/
build/
coverage/
node_modules/
.cache/
# Legacy areas (exclude entirely)
src/
server/
api/
app/
# Allow scripts for Phase-1 validation
!scripts/**
# Keep only config files for now
!.eslintrc*
!eslint.config.js
