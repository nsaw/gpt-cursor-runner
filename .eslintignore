dist/
build/
coverage/
node_modules/
.cache/
packages/
apps/
src/
api/
app/
# Exclude server files with syntax errors (need separate fix)
server/handlers/
server/routes/
# keep these INCLUDED (negations must be last to override)
!scripts/**
!config/**
!src-nextgen/**
!server/**
